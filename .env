# Environment variables loaded by `vercel dev` for local development.
# IMPORTANT: This file MUST be listed in .gitignore to prevent committing secrets!

# --- OpenAI API Configuration ---
# Replace 'your_openai_api_key_here' with your actual OpenAI API key.
# This key is required by the backend function (api/generate.ts) to interact with the OpenAI image generation API.
OPENAI_API_KEY=your_openai_api_key_here